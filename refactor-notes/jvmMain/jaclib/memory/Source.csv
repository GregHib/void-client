current_name,recommended_name,current_params,recommended_params,confidence,notes
getSize,getSize,,,high,"Returns the buffer's total capacity in bytes. Same contract as Buffer.getSize(); NativeBuffer implements both Buffer and Source with a single shared override."
getAddress,getAddress,,,high,"Returns the native memory address/pointer backing this buffer. Same contract as Buffer.getAddress(); a read-only subset interface used wherever only address+size (not writing) is needed, e.g. Stream's constructor takes a Buffer but GlBufferObject.method2121 takes a Source."
