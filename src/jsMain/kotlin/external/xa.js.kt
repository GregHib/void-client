package external

import Disposable
import Marker

actual class xa actual constructor(i: Int, i_1_: Int) : Marker, Disposable {
    actual var nativeid: Long
        get() = TODO("Not yet implemented")
        set(value) {}

    actual fun r(i: Int, i_0_: Int) {
    }

    actual fun va(l: Long, bool: Boolean) {
    }

    actual override fun w(bool: Boolean) {
    }

    actual fun finalize() {
    }
}