/**
 * JS actual for [class348Sub42Method3163]: resets game-state arrays that are
 * JVM-only in this phase. On JS these arrays don't exist yet, so this is a no-op
 * stub — the game reset path won't be exercised until the relevant classes are
 * moved to commonMain in a later phase.
 */
actual fun class348Sub42Method3163(i: Byte) {
    // no-op until Class154/Class259/Class348_Sub17 are ported to commonMain
}
